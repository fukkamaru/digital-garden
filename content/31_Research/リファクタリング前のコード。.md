---
title: リファクタリング前のコード。
aliases:
  - リファクタリング前のコード。
type: fleeting
created: 2026-08-13T11:11:04+09:00
updated: 2026-09-01T19:28:44+09:00
id: 20260813-111104
permalink:
draft: true
tags:
  - ai-generated
---
# リファクタリング前のコード。

下記のパワークエリのコードについて、ステップ名「 区切り記号の前に挿入されたテキスト」～ 「#"名前が変更された列 "」と同様の処理を、カラム名「入荷日付」を対象とした処理をコードを追加してください。


■パワークエリのコード
let
    ソース = Csv.Document(File.Contents("C:\Users\kyoupatty029\myproject\inventory\load_fils\purchase_csv\purchase_20201101-20211031.csv"),[Delimiter=",", Columns=28, Encoding=932, QuoteStyle=QuoteStyle.None]),
    昇格されたヘッダー数 = Table.PromoteHeaders(ソース, [PromoteAllScalars=true]),
    変更された型 = Table.TransformColumnTypes(昇格されたヘッダー数,{{"仕入部門コード", type text}, {"仕入部門名", type text}, {"仕入先コード", type text}, {"仕入先略名", type text}, {"伝票番号", type text}, {"行番号", type text}, {"仕入日付", type text}, {"入荷日付", type text}, {"仮伝票区分名", type text}, {"伝票区分", type text}, {"取引区分", type text}, {"商品コード", type text}, {"商品名", type text}, {"入数", Int64.Type}, {"荷姿数", Int64.Type}, {"総数", Int64.Type}, {"単位", type text}, {"単価", type number}, {"仕入金額", Int64.Type}, {"摘要", type text}, {"税処理区分名称", type text}, {"単価採用情報", type text}, {"税抜金額（符号無）", Int64.Type}, {"消費税（符号無）", Int64.Type}, {"税込金額（符号無）", Int64.Type}, {"税抜金額（符号有）", Int64.Type}, {"消費税（符号有）", Int64.Type}, {"税込金額（符号有）", Int64.Type}}),
    区切り記号の前に挿入されたテキスト = Table.AddColumn(変更された型, "仕入月", each Text.BeforeDelimiter([仕入日付], "/"), type text),
    置き換えられた値 = Table.ReplaceValue(区切り記号の前に挿入されたテキスト," ","0",Replacer.ReplaceText,{"仕入月"}),
    区切り記号の後に挿入されたテキスト = Table.AddColumn(置き換えられた値, "仕入日", each Text.AfterDelimiter([仕入日付], "/"), type text),
    置き換えられた値1 = Table.ReplaceValue(区切り記号の後に挿入されたテキスト," ","0",Replacer.ReplaceText,{"仕入日"}),
    追加された条件列 = Table.AddColumn(置き換えられた値1, "仕入年", each if [仕入月] = "11" then 2020 else if [仕入月] = "12" then 2020 else 2021),
    変更された型1 = Table.TransformColumnTypes(追加された条件列,{{"仕入年", type text}}),
    並べ替えられた列 = Table.ReorderColumns(変更された型1,{"仕入部門コード", "仕入部門名", "仕入先コード", "仕入先略名", "伝票番号", "行番号", "仕入日付", "入荷日付", "仮伝票区分名", "伝票区分", "取引区分", "商品コード", "商品名", "入数", "荷姿数", "総数", "単位", "単価", "仕入金額", "摘要", "税処理区分名称", "単価採用情報", "税抜金額（符号無）", "消費税（符号無）", "税込金額（符号無）", "税抜金額（符号有）", "消費税（符号有）", "税込金額（符号有）", "仕入年", "仕入月", "仕入日"}),
    挿入された日付 = Table.AddColumn(並べ替えられた列, "日付", each #date(Number.From([仕入年]), Number.From([仕入月]), Number.From([仕入日])), type date),
    削除された列 = Table.RemoveColumns(挿入された日付,{"仕入日付", "仕入年", "仕入月", "仕入日"}),
    #"名前が変更された列 " = Table.RenameColumns(削除された列,{{"日付", "仕入日"}})
in
    #"名前が変更された列 "
